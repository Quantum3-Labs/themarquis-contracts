query getEntities {
  entities(keys: ["*"]) {
    edges {
      node {
        keys
        models {
          __typename
          ... on ERC20Balance {
            account
            amount
          }
        }
      }
    }
  }
}
